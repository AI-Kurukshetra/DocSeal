import { RecipientDashboard } from "@/features/signing/components/recipient-dashboard";

export const metadata = { title: "My Signings — DocSeal" };

export default function SigningPage() {
  return <RecipientDashboard />;
}
