import { ResetPasswordForm } from "@/features/auth/components/reset-password-form";

export const metadata = { title: "Reset Password — DocSeal" };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
